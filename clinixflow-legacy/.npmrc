# Force npm to be used instead of pnpm/yarn
package-manager=npm
legacy-peer-deps=true
# Ensure optional dependencies are installed (needed for tailwindcss/oxide and lightningcss native bindings)
optional=true
